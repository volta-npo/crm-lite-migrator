openapi: 3.1.0
info:
  title: "CRM Lite Migrator API"
  version: "1.0.0"
  description: "Backend API for A migration checklist for moving customer lists from notebooks, spreadsheets, or inboxes into a lightweight CRM."
servers:
  - url: http://127.0.0.1:8787
    description: Python backend
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: Product identity and health status
  /score:
    post:
      summary: Score evidence payload
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Release packet
 