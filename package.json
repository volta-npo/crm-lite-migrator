{
  "name": "@volta-oss/crm-lite-migrator",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "A migration checklist for moving customer lists from notebooks, spreadsheets, or inboxes into a lightweight CRM.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "test:python": "PYTHONPATH=backend/python python3 -m unittest discover -s backend/python/tests",
    "test:backend": "npm run test:python"
  },
  "keywords": [
    "ai-automation",
    "digital-equity",
    "local-first",
    "oss",
    "v3-production",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/crm-lite-migrator.git"
  },
  "homepage": "https://volta-npo.github.io/crm-lite-migrator/",
  "bugs": {
    "url": "https://github.com/volta-npo/crm-lite-migrator/issues"
  },
  "author": "Volta OSS Division <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^24.0.0"
  }
}
